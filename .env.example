# Hedera operator (for HCS topic creation + message submission)
OPERATOR_ACCOUNT_ID=0.0.xxxxx
OPERATOR_PRIVATE_KEY=302e020100300506032b657004220420...

# Created once via CLI and then reused
HEDERA_TOPIC_ID=0.0.xxxxx

# Hedera JSON-RPC Relay endpoint (Hashio testnet default)
HEDERA_JSON_RPC_URL=https://testnet.hashio.io/api

# EVM account + deployed contract for invoice settlement
EVM_PRIVATE_KEY=0x...
CONTRACT_ADDRESS=0x...

# Optional deployment default (basis points)
DEFAULT_PLATFORM_FEE_BPS=100
